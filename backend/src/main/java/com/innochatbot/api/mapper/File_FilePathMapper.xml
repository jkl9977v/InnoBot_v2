<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.innochatbot.api.mapper.File_FilePathMapper">

<select id="filehashSelect" resultType="string" parameterType="string">
select hash from file where file_id = #{fileId}
</select>

<select id="fileIdSelect" parameterType="string">
select file_id
from file f
join file_path fp
on f.path_id = fp.path_id
where file_name like ('%', #{fileName},'%') 
and fp.path = #{path} 
</select>

</mapper>